these scripts require posix commands, like found
in Linux, FreeBSD, Cygwin, Mingw:
	Awk
	grep
	perl
	curl
	pipe operator
	sh

Start the program by running

./start.sh

it will put output in out.txt and error messages in err.txt
